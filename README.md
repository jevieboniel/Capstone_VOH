

# 🖥️ **How to Set Up This Project on Your Local Machine**

Follow these steps to run the project locally:

---

## 📌 **Prerequisites**

Make sure you have the following installed:

### ✔️ **Node.js (LTS version recommended)**

Check version:

```bash
node -v
```

### ✔️ **npm**

Included with Node.
Check version:

```bash
npm -v
```

---

# 🚀 **1. Clone the Project**

If the project is hosted on GitHub or any repository:

```bash
git clone <repository-url>
```

Example:

```bash
git clone https://github.com/jevieboniel/Capstone_VOH.git
```

Then navigate into the project folder:

```bash
cd my-react-app
```

---

# 📦 **2. Install Project Dependencies**

Inside the project directory, run:

```bash
npm install
```

This installs all packages listed in `package.json`.

---

# ▶️ **3. Start the Development Server**

To run the app locally:

```bash
npm start
```

Once started, open your browser:

👉 **[http://localhost:3000](http://localhost:3000)**

* The app reloads automatically on code changes
* Lint errors will appear in the terminal

---

# 🧪 **4. Running Tests**

```bash
npm test
```

This launches the interactive test runner.

---

# 🏗️ **5. Create a Production Build**

Build an optimized version of the app:

```bash
npm run build
```

The output is created inside the **build/** folder.
This version is ready to be deployed to hosting platforms.

---

# ⚙️ **6. Ejecting (Advanced and Optional)**

```bash
npm run eject
```

⚠️ **Warning: This is permanent.**
Ejecting gives full control over Webpack, Babel, ESLint configs.
After ejecting, you cannot revert automatically.

---

# 📚 **Learn More**

React documentation:
[https://reactjs.org/](https://reactjs.org/)

Create React App documentation:
[https://facebook.github.io/create-react-app/docs/getting-started](https://facebook.github.io/create-react-app/docs/getting-started)

---
